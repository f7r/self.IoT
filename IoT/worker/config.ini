[default]
worker_id = worker
log_path = /var/log/IoT

[mqtt]
broker_host = mosquitto
broker_port = 8883
keepalive = 60
parent_topic = self.IoT
use_tls = True
tls_ca_file = CA.pem
tls_cert_file = worker.pem
tls_key_file = worker.key
